/// Identifies content contained in the linked resource, that cannot be strictly
/// identified using a media type.
///
/// PartOf: [Json Schema](https://readium.org/webpub-manifest/schema/extensions/epub/properties.schema.json)
enum Contain {
  mathml,
  onix,
  remoteResources,
  js,
  svg,
  xmp,
}
